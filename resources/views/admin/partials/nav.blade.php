<div class="card" style="margin-bottom: 18px;">
    <div class="actions">
        <a class="btn btn-secondary" href="{{ route('admin.dashboard') }}">Admin Home</a>
        <a class="btn btn-secondary" href="{{ route('admin.sessions.index') }}">Sessions</a>
        <a class="btn btn-secondary" href="{{ route('admin.rooms.index') }}">Rooms</a>
        <a class="btn btn-secondary" href="{{ route('admin.speakers.index') }}">Speakers</a>
        <a class="btn btn-secondary" href="{{ route('admin.tracks.index') }}">Tracks</a>
    </div>
</div>
