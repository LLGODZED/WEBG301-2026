@extends('layouts.app')
@section('title', 'Manage Sessions')
@section('content')
<section class="section"><div class="container">@include('admin.partials.nav')<div class="actions" style="justify-content:space-between;"><h1>Sessions</h1><a class="btn" href="{{ route('admin.sessions.create') }}">Create session</a></div><div class="table-wrap"><table><thead><tr><th>Title</th><th>Time</th><th>Room</th><th>Speaker</th><th>Track</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach($sessions as $session)<tr><td>{{ $session->title }}</td><td>{{ $session->date_label }}<br>{{ $session->time_label }}</td><td>{{ $session->room->name ?? '-' }}</td><td>{{ $session->speaker->name ?? '-' }}</td><td>{{ $session->track->name ?? '-' }}</td><td><span class="badge">{{ $session->status }}</span></td><td class="actions"><a href="{{ route('admin.sessions.show', $session) }}">View</a><a href="{{ route('admin.sessions.edit', $session) }}">Edit</a><form method="POST" action="{{ route('admin.sessions.destroy', $session) }}">@csrf @method('DELETE')<button class="btn btn-danger btn-small" onclick="return confirm('Delete this session?')">Delete</button></form></td></tr>@endforeach</tbody></table></div>{{ $sessions->links() }}</div></section>
@endsection
